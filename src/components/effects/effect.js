import * as R from "@react-three/rapier";
import * as D from "@react-three/drei";
import { useEffects } from "./effects.hook";

const Effects = () => {
  const u = useEffects();

  return <></>;
};

export { Effects };
