import * as R from "@react-three/rapier";
import * as D from "@react-three/drei";
import { useTryeffects } from "./tryeffects.hook";

const Tryeffects = () => {
  const u = useTryeffects();

  return <></>;
};

export { Tryeffects };
